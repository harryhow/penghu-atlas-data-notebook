# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: Penghu Atlas - Soundscape Data Project
author: Harry Chen
logo: logo.png

copyright: "This work is licensed under Creative Commons Attribution 4.0 International License"

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force
  allow_errors: true # for resolve folium map rendering error

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/harryhow/penghu-atlas-data-notebook # Online location of your book
  path_to_book: .  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  baseurl: /penghu-atlas-data-notebook
  use_issues_button: true
  use_repository_button: true

parse:
  myst_enable_extensions:
    - colon_fence
    - dollarmath
    - amsmath
    - deflist
    - html_admonition
    - html_image
    - smartquotes
    - substitution
    - tasklist

sphinx:
  extra_extensions:
    - sphinxcontrib.mermaid

